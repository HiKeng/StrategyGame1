using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ลองรันโค้ดเป็นภาษาไทย : MonoBehaviour
{
    string ข้อความที่ต้องการพิมพ์ = "รันโค้ดด้วยภาษาไทยสำเร็จแล้ว เย้!";

    void Start()
    {
        Debug.Log(ข้อความที่ต้องการพิมพ์);
    }
}
